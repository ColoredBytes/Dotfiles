#!/usr/bin/env bash
# Author: Joshua Ross
# Purpose: aliases to use in wsl.


alias sc="grep ^[^#] $1" #(strips comments from file.) 
alias vim="nvim" # set vim to open neovim instead.
alias tree='tree --dirsfirst -F'
